spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  cache:
    type: none
  jpa:
    hibernate.ddl-auto: update